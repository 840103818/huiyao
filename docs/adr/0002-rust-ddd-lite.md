# ADR-0002：Rust 采用 DDD-lite

- 状态：已接受
- 日期：2026-07-25

## 背景

命令注册、业务编排、HTTP、存储、Keychain 和导出集中在少数大型文件，难以独立测试和审查安全边界。

## 决策

使用 `commands/application/domain/infrastructure` 四类边界，不引入完整 DDD 框架。命令只适配 Tauri；领域模型不依赖 Tauri；外部系统实现位于基础设施层。

## 影响

现有宏和私有辅助函数通过同命名空间分片保持兼容。新增业务必须进入对应层，逐步减少命令文件中的编排代码。
