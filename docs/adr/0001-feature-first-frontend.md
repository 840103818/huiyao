# ADR-0001：前端采用 Feature-first

- 状态：已接受
- 日期：2026-07-25

## 背景

原前端按 `components/lib` 横向分层，业务组件、测试和领域工具分散，`App.tsx` 与单一 Bridge 持续增长。

## 决策

组件、Hook、领域工具和测试按图片输入、生成、分析、提示词、历史、设置和诊断聚合。跨端契约进入 `shared/contracts`，桌面边界进入 `infrastructure/tauri`。

## 影响

功能修改具有稳定归属；共享层必须保持无业务反向依赖。当前不引入额外状态管理或样式框架。
