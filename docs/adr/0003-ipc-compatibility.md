# ADR-0003：IPC 兼容优先

- 状态：已接受
- 日期：2026-07-25

## 背景

应用已存在用户设置、历史、Keychain 项和前后端命令调用，大规模目录调整不能触发数据迁移或运行时不兼容。

## 决策

工程重构不改变命令名、参数名、Channel 事件、serde 字段、Keychain 服务名、Bundle ID 和应用数据路径。跨端结构变化必须同步 TypeScript、Rust、浏览器降级和测试。

## 影响

内部目录可以独立演进，但所有边界变更需要显式迁移、兼容测试和发布说明。
