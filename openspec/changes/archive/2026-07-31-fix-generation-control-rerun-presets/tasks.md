## 1. 生成与停止协调

- [x] 1.1 在应用状态层收敛直接生成、重新分析和队列处理的运行生命周期，统一维护停止锁存、活动 interaction ID、工作线程收尾和幂等清理，并用单元测试验证重复停止不产生重复状态更新。
- [x] 1.2 在图片准备完成后、请求 started、反推完成后和自动优化开始前检查停止锁存，确保停止前尚未登记及停止后迟到的请求立即取消；用确定性测试覆盖准备阶段、等待首字、流式、自动优化和 fallback 边界。
- [x] 1.3 调整队列停止流程，使两个并发请求全部取消、未启动任务不再取出、未完成任务最终写为 paused，并只在 worker 全部退出后清除 queueRunning 和显示“已停止”。
- [x] 1.4 保持 `cancel_reverse_prompt`、SSE Channel 和任务状态 IPC 兼容，审查 Rust 取消返回与前端错误映射；如无需 Rust 修改，在变更记录中明确现有取消测试覆盖结论。

## 2. 分析期间操作互斥

- [x] 2.1 从统一运行生命周期派生 `analysisLocked`，传递到 Toolbar、ProjectTaskSidebar、ImageWorkbench、ResultsWorkspace、快捷命令和响应式 Drawer，并确保分析开始前关闭可修改上下文的弹层。
- [x] 2.2 分析和停止收尾期间禁用项目、任务、导入、预设、图片、参数、修订、复制、优化、重新分析、导出、设置、日志及快捷命令，仅保留“停止生成”；验证正文滚动、文本选择和实时状态仍可用。
- [x] 2.3 将 `Cmd/Ctrl+Enter` 与 `Esc` 在锁定期间统一映射到停止流程，验证键盘焦点不会落入禁用控件，停止结束后恢复到稳定控件。

## 3. 原任务重新分析

- [x] 3.1 将已完成任务的默认重新生成改为复用原任务，使用临时 `rerunningTaskId` 展示“重新分析中”，不把 completed 任务写回队列状态且不创建新任务。
- [x] 3.2 严格结果成功后通过现有结果更新命令原子替换同一任务结果；停止、失败、断流或应用退出时清理临时状态并保留旧结果，测试任务 ID、任务数量、原图、标题、收藏和标签保持不变。
- [x] 3.3 将默认入口统一命名为“重新分析”，把现有创建关联副本保留为明确独立操作，并测试只有显式副本操作才增加任务。

## 4. 预设选择器

- [x] 4.1 将详细程度和自动优化字段改为非 label 容器与显式 `aria-labelledby` 关联，删除失效的预设局部 Trigger 层级补丁并继续复用全局 Arco 弹层层级。
- [x] 4.2 扩充 ProjectTaskSidebar 测试，断言 Select 不位于原生 label 内，并覆盖详细程度、自动优化、附加要求和保存快照；执行 `npm test -- --run src/features/projects/ProjectTaskSidebar.test.tsx`。
- [x] 4.3 使用真实浏览器验证鼠标单击、触控板、键盘打开与选择、Modal 焦点锁定和关闭后焦点恢复，确认菜单不会立即关闭且可交互。

## 5. 审查与回归测试

- [x] 5.1 增加队列与 App 测试，覆盖停止发生在 interaction ID 前后、两个并发、重复停止、停止后继续、完成后原任务重跑、失败回滚和分析期间操作互斥。
- [x] 5.2 使用 Mock HTTP 覆盖慢连接、无限 SSE、fallback、断流和自动优化切换阶段的取消，确认停止后没有新模型请求且部分结果不被持久化。
- [x] 5.3 运行 Rust 测试，确认取消、响应限制、任务结果原子更新和现有状态机无回归；验证本变更未修改 SQLite Schema、serde 字段、命令名、Keychain 或原图路径。
- [x] 5.4 检查停止和重跑日志只记录请求 ID、状态、耗时及其他脱敏运行元数据，不包含图片字节、Data URL、提示词、模型正文或本机路径。

## 6. 文档与界面基线

- [x] 6.1 更新 README、用户工作台说明、交互规范、前端架构和 IPC 说明，记录唯一停止操作、paused 重新请求语义、原任务重新分析及显式创建副本的区别。
- [x] 6.2 使用脱敏预览数据覆盖 `docs/assets/ui/current/` 中 1440x900 与 1120x720 的明暗主题分析锁定、正在停止、停止完成和预设下拉状态截图，并更新截图索引。

## 7. 最终验证

- [x] 7.1 执行 `npm run check`，确保 TypeScript、Vitest、构建和 OpenSpec 严格校验全部通过。
- [x] 7.2 执行 Tauri 原生冒烟，验证单项与双并发停止、停止后继续、同任务重新分析、失败保留旧结果和预设选择保存。
- [x] 7.3 执行 `git diff --check` 和 `codegraph sync`，审查差异只覆盖批准范围且代码图谱已同步。
